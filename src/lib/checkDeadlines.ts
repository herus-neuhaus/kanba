export async function checkTaskAutomations(agencyId: string) {
  // Moved to backend Cron Job (server/src/jobs/deadlineCheck.ts)
  console.log('Automations are now handled by the backend cron job.');
}
